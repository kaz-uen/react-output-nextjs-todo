import React from "react";
import CreateTodo from "../../components/CreateTodo";
import App from "../app";

const page = () => {
  return (
    <App>
      <CreateTodo />
    </App>
  );
};

export default page;
